---
interface Props {
  className?: string;
}
const { className = "" } = Astro.props;
---

<button
  type="button"
  class={`btn-conocenos inline-flex items-center justify-center cursor-pointer px-8 py-2.5 ${className}`}
>
  Contáctenos
</button>
